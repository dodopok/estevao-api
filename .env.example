# Configuração de exemplo para desenvolvimento
# Copie este arquivo para .env e ajuste conforme necessário

# Rails
RAILS_ENV=development
SECRET_KEY_BASE=development_secret_key_base_change_in_production

# Database (PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/estevao_api_development

# Para Docker Compose, use:
# DATABASE_URL=postgres://postgres:postgres@db:5432/estevao_api_development

# Firebase Authentication
# Obtenha o Project ID no Firebase Console: https://console.firebase.google.com/
# (Settings > Project settings > General > Project ID)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Admin SDK (para deletar usuários)
# Obtenha no Firebase Console: Settings > Service Accounts > Generate new private key
# Do arquivo JSON baixado, extraia os valores de "client_email" e "private_key"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Audio CDN Configuration
# AUDIO_CDN_HOST: URL completa do CDN para servir arquivos de áudio
# Se configurado, os audio_urls retornarão URLs completas com este host
# Exemplo: https://d1234abcd.cloudfront.net ou https://cdn.estevao.app
# Em desenvolvimento, deixe vazio para usar paths relativos
AUDIO_CDN_HOST=

# APP_HOST: URL da aplicação (OBRIGATÓRIO em produção)
# Usado para gerar URLs de Active Storage (fotos de perfil) e quando CDN não está configurado
# IMPORTANTE: Configure em produção para evitar URLs inválidas
# Exemplo: https://api.caminhoanglicano.com.br
APP_HOST=http://localhost:3000

# Development Settings
# MOCK_PREMIUM: Quando definido como "true" em development, todos os usuários logados
# são tratados como premium, permitindo testar funcionalidades premium sem assinatura
# MOCK_PREMIUM=true

# Datadog Configuration
# Get DD_API_KEY from: https://app.datadoghq.com/organization-settings/api-keys
# DD_SITE is your Datadog site (datadoghq.com, datadoghq.eu, etc.)
DD_API_KEY=your_datadog_api_key_here
DD_SITE=datadoghq.com
DD_ENV=development
DD_SERVICE=estevao-api
DD_AGENT_HOST=datadog-agent
DD_DOGSTATSD_PORT=8125
DD_TRACE_ENABLED=true
DD_LOGS_INJECTION=true

# Datadog Profiling (optional, enable in production for performance insights)
DD_PROFILING_ENABLED=false
